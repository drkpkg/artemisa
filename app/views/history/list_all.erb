<div class="container">
  <div class="col-md-12 row">

    <br>
    <div class="container">
      <div class="btn-group">
        <a href="new" class="btn btn-success">
          <i class="fa fa-plus"></i>
          Nuevo registro de inventario
        </a>
        <!--%= link_to "Descargar como CSV", assets_list_all_path(format: "csv"), class: "btn btn-info" %-->
        <!--%= link_to "Descargar como Plantilla de Excel", assets_list_all_path(format: "xls"), class: "btn btn-info" %-->
      </div>
    </div>
    <br>

    <div class="panel panel-default">
      <!-- Default panel contents -->
      <div class="panel-heading">Activos fijos</div>

      <table class="table">
        <thead>
        <tr>
          <th>Usuario</th>
          <th>Acción</th>
          <th>Vista</th>
          <th>Fecha de uso</th>
        </tr>
        </thead>
        <tbody>
        <% @histories.each do |actual_index| %>
            <tr>
              <td id="actual-name-<%= actual_index.id %>"><%= actual_index.user_id %></td>
              <td id="actual-desc-<%= actual_index.id %>"><%= actual_index.action %></td>
              <td id="actual-price-<%= actual_index.id %>"><%= actual_index.view %></td>
              <td id="actual-priceA-<%= actual_index.id %>"><%= actual_index.created_at %></td>
            </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>